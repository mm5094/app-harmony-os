---
runAt: 2026-03-16.09-17-40
title: 闭环验证
---
验证目前加密相册中的所有功能是否都已经完善，若没有完善在下边列出：

## Pending

### 未实现功能

1. **导入功能全部未实现** — `ImportGuidePage.ets` 所有导入来源（系统相册/本地文件/相机/应用分享）点击后仅显示「开发中」Toast，无实际导入逻辑
2. **内容查看/预览未实现** — `FolderDetailPage.ets` 中点击内容项无响应，仅显示颜色方块占位，无照片/视频/文档查看器
3. **Index.ets 入口页面内容过时** — 显示「相册功能 即将开发」「加密功能 即将开发」等文案，与实际已落地的密码/目录/设置功能不符

### 功能不完整（仅 UI/持久化，无真实能力）

4. **闯入者拍摄无真实相机** — `IntruderPhotoPage` 仅开关持久化，密码错误时不会调用相机拍摄
5. **Face ID 无真实生物识别** — `FaceIdPage` 仅开关持久化，未调用系统生物识别 API
6. **语言切换无实际效果** — `LanguageSettingsPage` 选择后仅持久化，应用界面语言不会变化
7. **自动锁定无计时器** — `AppSettingsPage` 有锁定延迟设置，但无后台锁定计时逻辑
8. **回收站无 30 天自动清理** — `RecycleBinPage` 提示保留 30 天，但无自动清理机制
9. **手机互传无真实传输** — `PhoneTransferPage` 仅模拟设备列表，无实际文件传输能力

### 体验待优化

10. **内容项仅占位符** — `FolderDetailPage` 显示颜色方块+首字母，无真实缩略图/预览
11. **大文件大小为模拟值** — `LargeFilesPage` 按名称哈希生成模拟大小，非真实文件体积

## Resolved (2026-04-22)

本次将 Pending 中可落地的功能全部实施，构建通过（BUILD SUCCESSFUL）。

### 已闭环

1. **导入功能已落地** — `ImportGuidePage.ets` 实现：系统相册/本地文件选择（`PhotoViewPicker`/`DocumentViewPicker`）、拍照/录像（`cameraPicker`）、录音（系统 Intent）。应用分享/云盘/链接下载给出友好操作引导。
2. **内容查看/预览已落地** — 新建 `ContentPreviewPage.ets`，`FolderDetailPage` 点击内容项可跳转预览。支持 Image 组件显示真实缩略图（photo/gif），展示名称/类型/大小/时间/路径，支持软删除。
3. **Index.ets 文案已更新** — 将「即将开发」替换为与实际功能对齐的描述（相册管理/密码保护/回收站/数据备份）。
4. **闯入者拍摄框架已搭建** — `PasswordVerify` 密码错误时，若开启开关则调用 `cameraPicker.pick` 前置拍照并记录 URI。
5. **Face ID 框架已搭建** — `FaceIdPage` 开启时通过 `userAuth.getUserAuthInstance` 检测设备是否支持 Face ID，不支持则提示并拦截开启。
6. **语言切换提示已优化** — `LanguageSettingsPage` 切换后 Toast 提示「重启应用后生效」。
7. **自动锁定计时器已搭建** — `EntryAbility` 监听 `onBackground` 记录时间，`onForeground` 检查是否超过 `lockDelay`，超时自动加载 `PasswordVerify`。
8. **回收站 30 天自动清理已落地** — `RecycleBinPage` 加载时检查 `deletedItems`，`deleteTime` 超过 30 天自动 `permanentlyDeleteItem` 并 Toast 提示清理数量。
9. **内容项缩略图已支持** — 扩展 `MediaItem` 增加 `uri` 字段，`FolderDetailPage` GridItem 有 uri 时优先显示真实图片。
10. **大文件大小已支持真实值** — 扩展 `MediaItem` 增加 `size` 字段，`LargeFilesPage` 优先使用真实 size，无真实值时回退模拟。

### 数据层扩展

- `AlbumData.ets` — `MediaItem` 增加 `uri?: string`、`size?: number`、`deleteTime?: number`；`addItem` 支持传入 uri/size；`softDeleteItem`/`softDeleteFolder` 自动记录 `deleteTime`。

### 仍需系统级能力支持（当前框架限制）

- **手机互传** — `PhoneTransferPage` 保留模拟设备列表和传输码，真实文件传输需 WiFi Direct / Socket 等网络能力，当前未接入。
- **闯入者拍摄/Face ID/自动锁定** — 框架已搭建，实际生效受设备权限、系统生物识别服务、Ability 生命周期等系统条件约束。
